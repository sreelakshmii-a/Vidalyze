# ── Runtime ─────────────────────────────────────────────────────────────────
Flask==3.0.3
Flask-Limiter==4.1.1
google-api-python-client==2.126.0
aiohttp==3.9.5
cachetools==5.3.3
python-dotenv==1.0.1
TextBlob==0.18.0.0

# Production WSGI server (Linux / Docker only — not compatible with Windows)
# Used by the Dockerfile CMD. For local Windows dev: python app.py
gunicorn==21.2.0

# ── Development & CI ─────────────────────────────────────────────────────────
pytest==8.2.0
pytest-cov==5.0.0
ruff==0.4.4
